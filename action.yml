name: runs-on freebsd-latest
description: 💻 Emulate 'runs-on: freebsd-latest' for GitHub Actions
author: Jacob Hummer <jcbhmr@outlook.com>
branding:
  icon: terminal
  color: green
runs:
  using: composite
  steps:
    - shell: bash
      run: |
        echo '::error::You need to use the reusable workflow! \
        Check out the website for a getting started guide. \
        https://jcbhmr.github.io/runs-on-freebsd-latest/'
        exit 1
